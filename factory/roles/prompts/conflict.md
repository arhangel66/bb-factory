Your work does not merge into the project: `{main}` moved while you worked.

```
{conflict}
```

The task is yours again. In your worktree run `git merge {main}`, resolve every conflict, check the work still
runs, then call `handoff` again. Leave the merge uncommitted: the board commits it for you.
